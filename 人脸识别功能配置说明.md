# 人脸识别功能配置说明

## 📋 后端已完成的工作

### 1️⃣ 添加依赖（pom.xml）
- ✅ 阿里云人脸识别SDK (`facebody20191230`)
- ✅ 阿里云OSS SDK (`aliyun-sdk-oss`)
- ✅ 阿里云核心SDK (`tea-openapi`, `tea-util`)

### 2️⃣ 数据库修改
- ✅ 在 `users` 表添加 `face_image_url` 字段
- ✅ 执行脚本：`add_face_image_url.sql`

### 3️⃣ 后端代码
- ✅ `FaceRecognitionService` - 人脸识别核心服务
- ✅ `User` 实体添加 `faceImageUrl` 字段
- ✅ `UserController.uploadFaceImage()` - 上传人脸照片接口
- ✅ `AttendanceController.checkInWithFace()` - 人脸识别打卡接口
- ✅ `UserService.updateFaceImage()` - 更新人脸照片URL

### 4️⃣ 新增API接口

#### 上传人脸照片
```
POST /api/user/upload-face
Content-Type: multipart/form-data
Authorization: Bearer <token>

参数:
- file: 人脸照片文件（jpg/png）

响应:
{
  "ok": true,
  "message": "人脸照片上传成功",
  "url": "https://bucket.oss-cn-shanghai.aliyuncs.com/faces/1_1234567890.jpg"
}
```

#### 人脸识别打卡
```
POST /api/attendance/check-with-face
Content-Type: multipart/form-data
Authorization: Bearer <token>

参数:
- type: 打卡类型（1-签到，2-签退）
- faceImage: 当前拍摄的人脸照片

响应:
{
  "ok": true,
  "message": "人脸识别成功，打卡完成",
  "data": { ...考勤记录... }
}
```

---

## ⚙️ 配置步骤

### 第1步：获取阿里云 AccessKey

1. 登录阿里云控制台：https://ram.console.aliyun.com/manage/ak
2. 创建 AccessKey（记住 AccessKeyId 和 AccessKeySecret）
3. **重要**：妥善保管密钥，不要泄露

### 第2步：开通阿里云服务

#### 开通OSS（对象存储）
1. 访问：https://oss.console.aliyun.com/
2. 创建Bucket：
   - 名称：自定义（如 `sailtrack-faces`）
   - 区域：华东2（上海）
   - 读写权限：**公共读**（允许匿名访问图片）

#### 开通人脸识别服务
1. 访问：https://vision.console.aliyun.com/
2. 开通"人脸人体"服务
3. 选择"按量付费"（每月有免费额度）

### 第3步：配置后端

编辑 `backend/src/main/resources/application.properties`：

```properties
# 阿里云配置
aliyun.access-key-id=YOUR_ACCESS_KEY_ID
aliyun.access-key-secret=YOUR_ACCESS_KEY_SECRET
aliyun.oss.bucket-name=sailtrack-faces
aliyun.oss.endpoint=oss-cn-shanghai.aliyuncs.com
aliyun.facebody.endpoint=facebody.cn-shanghai.aliyuncs.com

# 人脸相似度阈值（80表示80%以上相似度才通过）
face.recognition.threshold=80.0
```

### 第4步：执行数据库迁移

```bash
mysql -u sailtrack -p < add_face_image_url.sql
```

### 第5步：重新编译运行

```bash
cd backend
mvn clean install
mvn spring-boot:run
```

---

## 🎨 前端需要实现的功能

### 功能1：个人中心 - 上传人脸照片

在 `Profile.vue` 中添加：

```vue
<template>
  <div class="face-upload">
    <h3>人脸识别设置</h3>
    <div v-if="userInfo.faceImageUrl">
      <img :src="userInfo.faceImageUrl" alt="人脸照片" style="width: 150px; height: 150px; border-radius: 50%;">
      <p>已上传人脸照片</p>
    </div>
    <el-upload
      action="http://localhost:8080/api/user/upload-face"
      :headers="{ Authorization: `Bearer ${token}` }"
      :on-success="handleUploadSuccess"
      :before-upload="beforeUpload"
      accept="image/*">
      <el-button type="primary">{{ userInfo.faceImageUrl ? '更换人脸照片' : '上传人脸照片' }}</el-button>
    </el-upload>
  </div>
</template>

<script setup>
const token = localStorage.getItem('token')

const beforeUpload = (file) => {
  const isImage = file.type.startsWith('image/')
  const isLt5M = file.size / 1024 / 1024 < 5

  if (!isImage) {
    ElMessage.error('只能上传图片文件')
    return false
  }
  if (!isLt5M) {
    ElMessage.error('图片大小不能超过 5MB')
    return false
  }
  return true
}

const handleUploadSuccess = (response) => {
  if (response.ok) {
    ElMessage.success('人脸照片上传成功')
    // 刷新用户信息
    loadUserInfo()
  }
}
</script>
```

### 功能2：Dashboard - 人脸识别打卡

在 `Dashboard.vue` 中添加：

```vue
<template>
  <div class="face-check-in">
    <!-- 摄像头预览 -->
    <video ref="videoRef" v-show="showCamera" autoplay style="width: 320px; height: 240px; border-radius: 8px;"></video>
    <canvas ref="canvasRef" style="display: none;"></canvas>
    
    <!-- 打卡按钮 -->
    <el-button-group>
      <el-button type="primary" @click="normalCheckIn">普通打卡</el-button>
      <el-button type="success" @click="faceCheckIn" v-if="hasFaceImage">人脸打卡</el-button>
    </el-button-group>
  </div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue'
import { ElMessage } from 'element-plus'
import { attendanceAPI, userAPI } from '../services/api'

const videoRef = ref(null)
const canvasRef = ref(null)
const showCamera = ref(false)
const hasFaceImage = ref(false)
let stream = null

// 检查用户是否已上传人脸照片
onMounted(async () => {
  const userInfo = await userAPI.getUserInfo()
  hasFaceImage.value = !!userInfo.data.faceImageUrl
})

// 普通打卡
const normalCheckIn = async () => {
  try {
    await attendanceAPI.checkIn(1) // 1-签到
    ElMessage.success('打卡成功')
  } catch (error) {
    ElMessage.error(error.message)
  }
}

// 人脸识别打卡
const faceCheckIn = async () => {
  try {
    // 打开摄像头
    showCamera.value = true
    stream = await navigator.mediaDevices.getUserMedia({ video: true })
    videoRef.value.srcObject = stream
    
    // 等待3秒后自动拍照
    await new Promise(resolve => setTimeout(resolve, 3000))
    
    // 拍照
    const canvas = canvasRef.value
    const video = videoRef.value
    canvas.width = video.videoWidth
    canvas.height = video.videoHeight
    const ctx = canvas.getContext('2d')
    ctx.drawImage(video, 0, 0)
    
    // 关闭摄像头
    stream.getTracks().forEach(track => track.stop())
    showCamera.value = false
    
    // 转换为Blob并上传
    canvas.toBlob(async (blob) => {
      const formData = new FormData()
      formData.append('type', 1) // 签到
      formData.append('faceImage', blob, 'face.jpg')
      
      const token = localStorage.getItem('token')
      const response = await fetch('http://localhost:8080/api/attendance/check-with-face', {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${token}`
        },
        body: formData
      })
      
      const data = await response.json()
      
      if (data.ok) {
        ElMessage.success(data.message)
      } else {
        ElMessage.error(data.message)
      }
    }, 'image/jpeg')
    
  } catch (error) {
    ElMessage.error('人脸识别失败: ' + error.message)
    if (stream) {
      stream.getTracks().forEach(track => track.stop())
    }
    showCamera.value = false
  }
}

// 组件卸载时关闭摄像头
onUnmounted(() => {
  if (stream) {
    stream.getTracks().forEach(track => track.stop())
  }
})
</script>
```

### 功能3：API服务扩展

在 `services/api.js` 中添加：

```javascript
export const userAPI = {
  // ...现有方法...
  
  // 上传人脸照片
  uploadFaceImage: (file) => {
    const formData = new FormData()
    formData.append('file', file)
    const token = localStorage.getItem('token')
    
    return fetch('http://localhost:8080/api/user/upload-face', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`
      },
      body: formData
    }).then(res => res.json())
  }
}

export const attendanceAPI = {
  // ...现有方法...
  
  // 人脸识别打卡
  checkInWithFace: (formData) => {
    const token = localStorage.getItem('token')
    
    return fetch('http://localhost:8080/api/attendance/check-with-face', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`
      },
      body: formData
    }).then(res => res.json())
  }
}
```

---

## 💰 费用说明

### 阿里云OSS
- 存储费用：约 ¥0.12/GB/月
- 流量费用：约 ¥0.50/GB（外网下行）
- 预计：每月 < ¥10

### 阿里云人脸识别
- 免费额度：每月 500 次
- 超出后：¥0.0015/次
- 预计：100人公司，每天2次打卡，每月约6000次 = ¥9

**总计**：每月约 ¥20 左右

---

## ⚠️ 注意事项

1. **隐私保护**：需要用户同意采集人脸信息
2. **降级方案**：人脸识别失败时可使用普通打卡
3. **网络依赖**：需要稳定的网络连接
4. **照片质量**：建议正面、光线充足的照片
5. **相似度阈值**：可根据实际情况调整（70-90）

---

## 🧪 测试流程

1. ✅ 配置阿里云 AccessKey
2. ✅ 执行数据库迁移
3. ✅ 重启后端服务
4. ✅ 在个人中心上传人脸照片
5. ✅ 在Dashboard使用人脸打卡
6. ✅ 检查考勤记录是否正常

---

## 📞 技术支持

如有问题，请检查：
1. 阿里云 AccessKey 是否正确
2. OSS Bucket 权限是否为"公共读"
3. 人脸识别服务是否已开通
4. 后端日志中的错误信息
