# 环境变量配置说明

本项目使用 `.env` 文件管理敏感配置信息，确保私密数据不会被提交到版本控制系统。

## 🔐 安全说明

- ✅ `.env` 文件已添加到 `.gitignore`，不会被提交到 Git
- ✅ 提供了 `.env.example` 模板文件，用于团队协作
- ⚠️ **请勿将 `.env` 文件提交到版本控制系统**
- ⚠️ **请勿在代码中硬编码敏感信息**

## 📦 后端配置

### 1. 创建 .env 文件

在 `backend/` 目录下创建 `.env` 文件：

```bash
cd backend
cp .env.example .env
```

### 2. 配置环境变量

编辑 `backend/.env` 文件，填入实际的配置信息：

```env
# 数据库配置
DB_HOST=your_database_host
DB_PORT=3306
DB_NAME=sailtrack
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# 邮件服务配置（163邮箱）
MAIL_HOST=smtp.163.com
MAIL_PORT=465
MAIL_USERNAME=your_email@163.com
MAIL_PASSWORD=your_email_auth_code

# 阿里云配置
ALIYUN_ACCESS_KEY_ID=your_aliyun_access_key_id
ALIYUN_ACCESS_KEY_SECRET=your_aliyun_access_key_secret
ALIYUN_OSS_BUCKET_NAME=your_oss_bucket_name
ALIYUN_OSS_ENDPOINT=oss-cn-hongkong.aliyuncs.com
ALIYUN_FACEBODY_ENDPOINT=facebody.cn-shanghai.aliyuncs.com

# 人脸识别配置
FACE_RECOGNITION_THRESHOLD=80.0

# JWT配置
JWT_SECRET=your_jwt_secret_key_at_least_32_characters
JWT_EXPIRATION=3600000
```

### 3. 配置项说明

#### 数据库配置
- `DB_HOST`: 数据库服务器地址
- `DB_PORT`: 数据库端口（默认 3306）
- `DB_NAME`: 数据库名称
- `DB_USERNAME`: 数据库用户名
- `DB_PASSWORD`: 数据库密码

#### 邮件服务配置
- `MAIL_HOST`: SMTP 服务器地址（163邮箱使用 `smtp.163.com`）
- `MAIL_PORT`: SMTP 端口（SSL 使用 465）
- `MAIL_USERNAME`: 邮箱地址
- `MAIL_PASSWORD`: 邮箱授权码（**不是邮箱密码**）

**如何获取163邮箱授权码：**
1. 登录 163 邮箱
2. 设置 → POP3/SMTP/IMAP
3. 开启 SMTP 服务
4. 获取授权码（需要手机验证）

#### 阿里云配置
- `ALIYUN_ACCESS_KEY_ID`: 阿里云 AccessKey ID
- `ALIYUN_ACCESS_KEY_SECRET`: 阿里云 AccessKey Secret
- `ALIYUN_OSS_BUCKET_NAME`: OSS 存储桶名称
- `ALIYUN_OSS_ENDPOINT`: OSS 服务端点
- `ALIYUN_FACEBODY_ENDPOINT`: 人脸识别服务端点

**如何获取阿里云 AccessKey：**
1. 登录阿里云控制台
2. 访问 https://ram.console.aliyun.com/manage/ak
3. 创建 AccessKey（建议使用 RAM 子账号）

#### JWT 配置
- `JWT_SECRET`: JWT 签名密钥（**至少 32 个字符**）
- `JWT_EXPIRATION`: Token 过期时间（毫秒，默认 3600000 = 1小时）

### 4. 依赖说明

项目使用 `dotenv-java` 库加载 `.env` 文件：

```xml
<dependency>
    <groupId>io.github.cdimascio</groupId>
    <artifactId>dotenv-java</artifactId>
    <version>3.0.0</version>
</dependency>
```

在 `BackendApplication.java` 中自动加载环境变量。

## 🌐 前端配置

### 1. 创建 .env 文件

在 `web/` 目录下创建 `.env` 文件：

```bash
cd web
cp .env.example .env
```

### 2. 配置环境变量

编辑 `web/.env` 文件：

```env
# API配置
VITE_API_BASE_URL=http://localhost:8080/api
```

### 3. 配置项说明

- `VITE_API_BASE_URL`: 后端 API 基础地址

**注意：** Vite 要求环境变量必须以 `VITE_` 开头才能在客户端代码中访问。

### 4. 使用方式

在前端代码中通过 `import.meta.env` 访问：

```javascript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api'
```

## 🚀 运行项目

### 后端

```bash
cd backend
mvn clean install
mvn spring-boot:run
```

### 前端

```bash
cd web
npm install
npm run dev
```

## 🔄 环境切换

### 开发环境
使用 `.env` 文件（本地开发）

### 生产环境
- **方式1**: 在服务器上设置系统环境变量
- **方式2**: 使用 `.env.production` 文件
- **方式3**: 在 Docker 容器中通过环境变量注入

## ❓ 常见问题

### 1. 后端无法读取 .env 文件？
- 确保 `.env` 文件位于 `backend/` 目录下
- 检查文件权限
- 查看启动日志中的加载提示

### 2. 前端环境变量不生效？
- 确保变量名以 `VITE_` 开头
- 修改 `.env` 后需要重启开发服务器
- 使用 `console.log(import.meta.env)` 检查变量

### 3. 邮件发送失败？
- 确认使用的是授权码，不是邮箱密码
- 检查 SMTP 服务是否开启
- 确认端口和 SSL 配置正确

### 4. 数据库连接失败？
- 检查数据库服务是否启动
- 确认主机地址、端口、用户名、密码正确
- 检查网络连接和防火墙设置

## 📝 团队协作

1. **首次克隆项目后**：
   ```bash
   # 后端
   cd backend
   cp .env.example .env
   # 编辑 .env 填入配置
   
   # 前端
   cd web
   cp .env.example .env
   # 编辑 .env 填入配置
   ```

2. **更新 .env.example**：
   - 添加新的环境变量时，同步更新 `.env.example`
   - 不要在 `.env.example` 中填入真实的敏感信息
   - 使用占位符或示例值

3. **文档更新**：
   - 新增环境变量时更新本文档
   - 说明变量用途和获取方式

## 🔒 安全最佳实践

1. ✅ 使用强密码和复杂的密钥
2. ✅ 定期更换密钥和密码
3. ✅ 使用 RAM 子账号管理阿里云权限
4. ✅ 最小权限原则
5. ❌ 不要在日志中输出敏感信息
6. ❌ 不要通过聊天工具传输 .env 文件
7. ❌ 不要截图包含敏感信息的配置文件

---

**最后更新**: 2025年12月18日  
**维护者**: SailTrack 开发团队
