version: '3.8'

services:
  # 后端服务
  backend:
    # 本地构建（开发环境）
    build:
      context: ./backend
      dockerfile: Dockerfile
    # 使用预构建镜像（生产环境，取消下面注释并注释上面的 build）
    # image: ghcr.io/chinality/zhineng_daka/backend:latest
    container_name: sailtrack-backend
    ports:
      - "8080:8080"
    environment:
      # 从 .env 文件读取环境变量
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - ALIYUN_ACCESS_KEY_ID=${ALIYUN_ACCESS_KEY_ID}
      - ALIYUN_ACCESS_KEY_SECRET=${ALIYUN_ACCESS_KEY_SECRET}
      - ALIYUN_OSS_BUCKET_NAME=${ALIYUN_OSS_BUCKET_NAME}
      - ALIYUN_OSS_ENDPOINT=${ALIYUN_OSS_ENDPOINT}
      - ALIYUN_FACEBODY_ENDPOINT=${ALIYUN_FACEBODY_ENDPOINT}
      - FACE_RECOGNITION_THRESHOLD=${FACE_RECOGNITION_THRESHOLD}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION=${JWT_EXPIRATION}
    networks:
      - sailtrack-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # 前端服务
  frontend:
    # 本地构建（开发环境）
    build:
      context: ./web
      dockerfile: Dockerfile
    # 使用预构建镜像（生产环境，取消下面注释并注释上面的 build）
    # image: ghcr.io/chinality/zhineng_daka/frontend:latest
    container_name: sailtrack-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - sailtrack-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  sailtrack-network:
    driver: bridge
